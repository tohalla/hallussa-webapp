export const base = "14px";
export const huge = `calc(${base} * 1.5)`;
export const large = `calc(${base} * 1.25)`;
export const small = `calc(${base} * .85)`;

export default {
  base,
  huge,
  large,
  small,
};
