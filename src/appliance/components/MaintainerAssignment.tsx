import React from "react";
import { AppliancePayload } from "../actions";

interface Props {
  appliance: AppliancePayload;
}

export default class MaintainerAssignment extends React.Component<Props> {
  public render() {
    return (
      <div />
    );
  }
}
